# example showing multiple assignment trick that only works when the assignment is done on a list of the SAME SIZE


cat = ['fat', 'black', 'loud']

size, color, disposition = cat

print(size, color, disposition)